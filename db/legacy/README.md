# Legacy migration scripts

migration แบบสะสมที่ใช้ตอนพัฒนาช่วงแรก (ส.ค. – ก.ย. 2026)

ปัจจุบัน **ไม่ถูกรันแล้ว** — ยุบรวมเป็น `db/init/01_init.sql`
ซึ่ง dump มาจาก database จริงหลังรันไฟล์เหล่านี้ครบทุกตัว

เก็บไว้ดูประวัติ schema เท่านั้น ห้ามรันซ้ำ
